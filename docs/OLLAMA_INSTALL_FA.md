# 🤖 راهنمای نصب هوش مصنوعی رایگان (Ollama)

## چرا Ollama؟

✅ **100% رایگان** - هیچ هزینه‌ای ندارد  
✅ **بدون API Key** - نیازی به ثبت نام یا اشتراک نیست  
✅ **محلی** - روی کامپیوتر خودتان اجرا می‌شود  
✅ **بدون محدودیت** - هر چقدر بخواهید استفاده کنید  
✅ **آفلاین** - بعد از دانلود مدل، نیازی به اینترنت ندارید  

## نصب (خیلی ساده!)

### ویندوز
1. برو به https://ollama.com/download
2. دانلود `OllamaSetup.exe`
3. اجرا کن و نصب کن (مثل هر برنامه دیگه)

### مک
```bash
brew install ollama
```

### لینوکس
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

## دانلود مدل (یکبار کافیه)

```bash
# سبک و سریع (توصیه میشه - فقط 2 گیگ)
ollama pull llama3.2:3b

# یا اگه کامپیوتر قویتری داری (7 گیگ)
ollama pull llama3.1:8b
```

## اجرا

```bash
# فقط یکبار اجرا کن، بعد همیشه در پس‌زمینه میمونه
ollama serve
```

## استفاده در GravityFramework

```python
from gravity_framework import GravityFramework

# اینقدر سادست که خودکار تشخیص میده!
framework = GravityFramework(ai_assist=True)

# حالا AI بهت کمک میکنه:
# ✓ میکروسرویس‌ها رو به هم وصل کنه
# ✓ دیتابیس‌ها رو آنالیز کنه
# ✓ خطاها رو تشخیص بده
# ✓ پیشنهاد بهینه‌سازی بده

# چک کن که AI فعاله
if framework.ai.enabled:
    print("🤖 AI فعال شد!")
else:
    print("⚠️ Ollama رو نصب کن: https://ollama.com/download")
```

## مدل‌های پیشنهادی

| مدل | حجم | سرعت | کیفیت | مناسب برای |
|-----|------|------|--------|-----------|
| `llama3.2:3b` | 2GB | خیلی سریع | خوب | توصیه می‌شود |
| `llama3.1:8b` | 7GB | متوسط | عالی | کامپیوترهای قوی‌تر |
| `deepseek-coder:6.7b` | 5GB | سریع | فوق‌العاده | کار با کد |
| `qwen2.5-coder:7b` | 7GB | سریع | عالی | آنالیز تکنیکال |

## تست کن

```bash
# بپرس ببین کار میکنه؟
ollama run llama3.2:3b "سلام! چطوری؟"
```

## سوالات متداول

**Q: چقدر اینترنت مصرف میکنه؟**  
A: فقط موقع دانلود مدل (یکبار). بعدش دیگه نیازی به اینترنت نیست!

**Q: کامپیوترم ضعیفه، کار میکنه؟**  
A: آره! با `llama3.2:3b` که فقط 2 گیگ حافظه میخواد.

**Q: واقعاً هیچ هزینه‌ای نداره؟**  
A: نه! 100% رایگان. نه API key، نه اشتراک، نه هیچی!

**Q: بهتر از ChatGPT هست؟**  
A: برای وصل کردن میکروسرویس‌ها و کارهای تخصصی، خیلی خوبه. و رایگانه!

## کمک

اگه مشکلی داشتی:
1. چک کن Ollama داره اجرا میشه: `ollama list`
2. مدل رو دانلود کرده باشی: `ollama pull llama3.2:3b`
3. سرویس Ollama فعال باشه: `ollama serve`

---

**نکته**: این AI روی کامپیوتر خودت اجرا میشه، نه روی سرور. پس:
- ✅ اطلاعاتت محفوظه
- ✅ سریع‌تره
- ✅ رایگانه
- ✅ آفلاین کار میکنه
